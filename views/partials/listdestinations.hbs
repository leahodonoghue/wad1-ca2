{{#if singleDestination.destinations}}

  <table class="ui unstackable striped table">
    <thead>
      <tr>
        <th>City</th>
        <th>Type</th>
        <th>Famous For</th>
        <th>image</th>
      </tr>
    </thead>
    <tbody>
      {{#each singleDestination.destinations}}
        <tr>
          <td>
            {{city}}
          </td>
          <td>
            {{type}}
          </td>
           <td>
            {{popular}}
          </td>
          <td>
            <img src="/public/{{image}}" alt="{{city}}" width="100">
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

  {{else}}
    <h3 class="ui message">
     Empty Destination List!
    </h3>

{{/if}}
